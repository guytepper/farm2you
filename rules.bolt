path / {
  read() { true }
  write() { true }
}

type Farm {
  name: String,
  address: String,
  email: String,
  website: String,
  facebook: String,
  phone: String,
  description: String,
  props: Object,
  submittedBy: String,
}

type User {
  isAdmin: Boolean,
}

path /users is User {}
